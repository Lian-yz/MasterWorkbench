# 更新日志

## v5.0.282 (2026-09-01 13:05)

- 自动发布版本 5.0.282

## v5.0.281 (2026-09-01 14:00)

### 新增
- 信息预览页全面改版：顶部改为「学业进度总览（缩窄）+ 论文概览 + 计划完成情况」三列布局，第二行改为「最近仿真 / 最近论文 / 最近组会」一行三列；内容过长自动省略但状态/日期等关键信息始终可见，点击卡片或列表项即可跳转对应页面
- 计划完成情况圆环升级交互：单击彩色色块弹出该状态全部任务详情（标题/数量/占比/截止时间），双击圆环直达计划中心「计划列表」；圆环下方不再显示图例文字
- 全局名称调整：仿真库→仿真列表、论文阅读库→论文阅读列表、论文库→论文列表、计划库→计划列表（含二级导航、模块标题、配置弹窗与导出文件名）

### 修复
- 修复信息预览页「最近组会」参与人员显示乱码（显示 JSON 原文）的问题：参与人员现统一显示为人名列表，兼容新旧数据格式
- 修复科研中心组会纪要中旧版手动输入参与人员的记录无法编辑的问题：编辑时自动兼容旧字符串格式，历史人员可正常回显、取消与保存

### 优化
- 学业进度总览编辑按钮图标重绘为便签+铅笔线性风格，编辑功能保持不变
- 移除信息预览页「最近计划」周历模块

## v5.0.280 (2026-09-01 10:00)

### 新增
- 计划库消息中心提醒全配置化：计划/任务配置弹窗新增「消息中心提醒设置」区块，可自定义提前提醒天数（默认 8 天）、4 档提醒颜色（临近/紧急/逾期/严重逾期）、参与提醒的计划状态（多选，默认排除已完成/放弃），逾期任务每天持续提醒直到状态变更为已完成

### 修复
- 修复顶部状态栏周数偶尔显示 null 的问题：增强日期解析防御逻辑，无效日期/undefined 周数回退到安全默认值

## v5.0.279 (2026-08-31 16:00)

### 新增
- 组会纪要参与人员分组管理：支持将人员按分组管理（增删改排序分组、人员归组），记录纪要时按分组多选参与人员
- 参与人员颜色优化：模板配置中颜色改为圆点展示，点击圆点弹窗可选系统颜色或自定义颜色

## v5.0.278 (2026-08-31 14:00)

### 新增
- 组会纪要参与人员标签化：支持在模板配置中自定义常参与人员（增删改排序+颜色），记录纪要时多选参与人员，每个人以不同底色标签展示
- 参与人员向后兼容：旧字符串格式的 participants 自动适配为标签展示

### 修复
- 彻底修复版本号显示不同步：根因是 `onMounted` 中 changelog 播种后第 4017 行将 `appVersion` 覆盖为 `changelog[0].version`，导致 `getVersion()` 同步的真实版本号被覆盖；改为桌面端不再从 changelog 取版本号，仅使用 Tauri `getVersion()` 真实版本号
- 计划库子任务时间清空：改为 `:value` + `@input` 显式绑定，避免 `v-model` 在 `datetime-local` 上的潜在冲突

## v5.0.277 (2026-08-31 12:00)

### 修复
- 彻底修复版本号显示不同步：根因是 `onMounted` 中 changelog 播种后第 4017 行将 `appVersion` 覆盖为 `changelog[0].version`，导致 `getVersion()` 同步的真实版本号被覆盖；改为桌面端不再从 changelog 取版本号，仅使用 Tauri `getVersion()` 真实版本号
- 计划库子任务时间清空：改为 `:value` + `@input` 显式绑定，避免 `v-model` 在 `datetime-local` 上的潜在冲突

## v5.0.276 (2026-08-31 10:00)

### 修复
- 修复版本号显示不同步：桌面端启动时第一时间用 Tauri `getVersion()` 获取真实安装版本号并同步到 localStorage，侧边栏与检查更新统一使用真实版本号，避免从旧版升级后 localStorage 残留旧版本号导致显示错误和误报更新

### 优化
- 计划库子任务下拉菜单时间自动清空：每次打开子任务选择面板时，截止时间输入框自动置空，便于快速选择新的状态和时间

## v5.0.275 (2026-08-31 01:00)

### 优化
- 计划到期提醒改为每次登录都检查：移除 `mw_deadlineReminded_` 每日只提醒一次的 localStorage 拦截，每次打开应用都会执行到期检测；同一天同一任务通过 `msgId` 含日期去重确保只发一条消息；≤3天临期（含截止当天）+ 逾期≤3天范围内每天登录都会提醒，除非状态变为"已完成"或"放弃"

## v5.0.274 (2026-08-31 00:30)

### 修复
- 彻底修复 GlassSelect 尺寸异常：根因是全局 `.input` 样式（padding/border/border-radius/width）污染了根 div，与内部 `.glass-select-trigger` 的样式叠加导致双重边框/内距；改为将 `selectClass` 拆分为布局类（`filter-select`/`filter-select-col` 绑根 div）和外观类（`input`/`batch-input`/`note-meta-select`/`status-select` 绑 trigger）
- 修复状态弹窗毛玻璃失效：弹窗 Teleport 到 body 后脱离 `.app-container` DOM 树，原 `.app-container.module-frosted .status-popup` 选择器不匹配，新增 `body.module-frosted .status-popup` 规则
- 修复检查更新无法检测到新版本：GitHub API 将安装包文件名中空格替换为点（`MasterWorkbench.5_5.0.273_x64-setup.exe`），Rust `extract_version` 函数新增 `MasterWorkbench.5_` 前缀匹配；Release 查询数量从 30 提升到 100（仓库已有 100+ 个 Release）

## v5.0.273 (2026-08-30 23:30)

### 修复
- 修复 GlassSelect 自定义下拉组件尺寸异常：移除根元素强制 `width:100%`，添加非 scoped 全局样式块响应 `filter-select`/`filter-select-col`/`wt-btn-select`/`note-meta-select`/`status-select`/`batch-input` 等外部 class 的宽度与 padding 规则，使各模块下拉框尺寸与原生 select 一致
- 修复论文库状态弹窗定位错乱：弹窗改用 `Teleport` 渲染到 body 避免被表格 `overflow-x:auto` 裁剪，表格滚动时自动关闭弹窗

## v5.0.272 (2026-08-30 22:00)

### 新增
- 全平台原生下拉菜单替换为自定义组件：论文管理/计划中心/仿真中心/财务中心/论文阅读/导航/日历/研究等模块共 64 处原生 select 全部替换为自定义 GlassSelect 组件，下拉面板完美适配毛玻璃效果
- 修复检查更新失效：改用 Tauri `getVersion()` 获取真实安装版本号，不再依赖 localStorage 中可能被提前更新的 `appVersion`

## v5.0.268 (2026-08-30 14:00)

### 修复
- 修复 GitHub 更新令牌每次重启后丢失的 BUG：store state 定义中缺少 `load('githubUpdateToken')` 初始加载，导致保存后重启读取不到，检查更新时回退为内置令牌；现已修复，令牌保存一次后持久生效

## v5.0.267 (2026-08-30 12:00)

### 优化
- 全平台下拉菜单/弹出层全面适配毛玻璃效果：颜色预设选择器、公式输入面板、PDF 阅读器浮层（选中操作/右键菜单/翻译/便签）、图片预览框、日历详情遮罩、财务批量编辑区等
- 全局原生 select 下拉框毛玻璃适配从 Plan 局部提升为全局，覆盖所有页面的 select.input / select.filter-select 等
- 各类弹窗遮罩层在毛玻璃模式下统一淡化（不再显示深色遮罩，透出真实背景图）

## v5.0.266 (2026-08-30 10:00)

### 新增
- 消息中心计划到期提醒4级颜色分级：距结束≤3天→淡红、≤8天→淡橙、逾期≥3天→淡紫、逾期≥7天→深红，颜色均为淡色不刺眼；状态变更为"已完成"或"放弃"后停止检测

### 优化
- 计划库状态/分类/层级/优先级下拉菜单（cell-picker）支持毛玻璃效果：毛玻璃模式下背景透明度跟随 --module-opacity 联动
- 修复 cell-picker 因 Teleport 到 body 脱离 .app-container DOM 树导致毛玻璃选择器不生效的问题：新增 body 级 class 同步机制

## v5.0.265 (2026-08-28 20:30)

### 优化
- 平台文档全部 14 章重写：技术栈补充 Tauri 2 桌面端框架描述，修正目录结构（新增 src-tauri/ 等），行数更正为实际值，数据存储章节区分桌面端/浏览器端双通道，构建部署章节改为 Tauri 桌面构建，运行环境章节改为桌面应用，致谢新增 Tauri/SheetJS/KaTeX/lucide 等依赖
- 文档修改时间常量更新至 v5.0.265

## v5.0.264 (2026-08-28 19:00)

### 优化
- 每日打卡设置弹窗：「+ 添加状态」由浏览器 prompt 弹窗改为弹窗内行内追加，新增后直接显示可编辑输入框，改名即时生效
- 计划/任务配置弹窗：「+ 添加」由浏览器 prompt 弹窗改为弹窗内行内追加，新增后直接显示可编辑输入框（名称/颜色/数值），修改即时生效
- 所有今日总结弹窗与总览最近打卡排版优化：标题（今日进展/遇到的问题/明日计划）改为加粗单独一行显示，内容在下一行完整展示、自动换行
- 「恢复默认」按钮统一为浅色圆角按钮样式，与弹窗内操作按钮风格一致，适配平台整体风格

## v5.0.263 (2026-08-28 18:00)

### 新增
- 每日打卡新增「每日打卡设置」按钮：可自定义今日底线任务的打卡状态（名称、颜色）、下拉菜单显示顺序，支持增删和排序
- 每日打卡新增「查看所有今日总结」按钮：弹窗展示全部历史今日总结记录，按日期降序排列
- 计划中心总览页面布局改造：左半侧显示近期任务，右半侧新增最近打卡（展示每日打卡中的今日总结内容）

### 优化
- 底线任务状态标签颜色、名称从硬编码改为从设置动态读取，修改即时生效
- 打卡状态选择浮层选项顺序跟随设置中的排序

## v5.0.262 (2026-08-28 17:10)

### 新增
- 仿真结果图片存放位置支持自定义：可点击「自定义」按钮选择任意文件夹作为图片存储位置，也可点击「恢复默认」回到系统数据目录

## v5.0.261 (2026-08-28 16:50)

### 新增
- 仿真库新增仿真记录支持上传结果图片：在「仿真结果」字段下方可添加多张图片，大小不限
- 桌面端图片自动落盘到本地数据目录「仿真结果」文件夹（`C:\Users\xxx\AppData\Roaming\com.mastersworkbench.app\硕士工作台数据\仿真结果`），浏览器端存 IndexedDB，不占 localStorage
- 仿真详情中结果图片可点击放大预览
- 删除仿真记录/仿真时同步清理关联的图片文件

## v5.0.260 (2026-08-28 09:30)

### 修复
- 修复版本号显示错误：老用户升级后版本号仍停留在 5.0.256，补充 5.0.257-5.0.259 版本更新链，升级后正确显示最新版本号
- 修复版本更新日志缺失：补充 5.0.257（activeTab 修复）、5.0.258（stores 编码损坏全面修复）、5.0.259（融合态品牌区修复）更新记录

### 优化
- 登录页面适配毛玻璃透明效果：支持显示自定义背景图片（与主界面背景图一致），登录卡片改为半透明毛玻璃样式，可以看见背景图片

## v5.0.259 (2026-08-27 21:40)

### 修复
- 修复融合态导航模式下顶部状态栏左侧不显示 Logo、惟创科技、版本号的问题（品牌区原仅在悬浮岛式模式下显示，现扩展至融合态模式）

## v5.0.258 (2026-08-27 21:00)

### 修复
- 彻底修复融合态和悬浮岛式导航下论文中心、计划中心页面内容不显示的问题
- 根因：stores/index.js 编码损坏导致多处 state 属性丢失和注释闭合标记被吞
- 修复 paperReadingsStore 丢失 visibleColumns/sortDirection/sortField（导致论文阅读 tab 崩溃）
- 修复 paperLibraryStore 丢失 tagColors（标签自定义颜色功能失效）
- 修复 planStore 丢失 dailyCheckins（每日打卡功能完全失效）
- 修复 settingsStore 丢失 sessionExpiredReason 和 bridgeSaveDir
- 修复 researchStore.addMeeting 丢失 doubts/nextSteps/relatedPaperId/createdAt 字段
- 修复 snapshotForRollback 方法被未闭合注释吞没导致 TypeError

## v5.0.257 (2026-08-27 18:30)

### 修复
- 修复融合态和悬浮岛式导航下论文中心、计划中心部分页面内容不显示的问题（根因：将各视图 activeTab 从局部 ref 改为绑定 store 的 computed 后，与 keep-alive 缓存机制交互导致响应式断裂；改为 ref + 双向 watch 同步 store，保留 Tab 记忆功能的同时恢复可靠渲染）

## v5.0.256 (2026-08-27 17:30)

### 新增
- 平台设置导航栏样式新增「融合态」：一级导航与二级导航融为一体，水平居中显示在原二级导航栏位置，取消左侧竖排侧栏
- 点击一级导航项后一级导航条消失，显示对应页面的二级导航条，二级导航条最左侧增加「主页」按钮可返回一级导航
- 无二级导航的页面（信息预览、平台设置）点击后保持一级导航条显示
- 二级导航 Tab 选择自动记忆，再次进入该页面时恢复上次选中的 Tab
- 融合态下毛玻璃效果与透明度完全沿用现有样式，不受影响

## v5.0.255 (2026-08-27 16:30)

### 优化
- 每日打卡底线任务状态交互调整：移除任务右侧的状态下拉框（仅保留编辑/删除按钮），改为点击任务左侧状态标签弹出三态菜单（已完成/未完成/放弃），三种状态文字在菜单项中左右居中显示，各有不同填充底色
- 状态菜单浮层适配毛玻璃效果，跟随模块透明度滑杆联动

## v5.0.254 (2026-08-27 14:00)

### 优化
- 信息预览页面（Dashboard）适配毛玻璃透明效果：学业进度总览、论文概览、最近仿真、最近论文、最近组会、最近计划、计划完成情况等卡片全部跟随模块透明度滑杆联动，内层列表项与周历格子半透明 + 清晰边框
- 每日打卡底线任务状态从「完成/未完成」两个按钮改为三态下拉菜单：未完成（橙色）、完成（绿色）、放弃（红色），不同状态有不同填充底色与边框色
- 底线达成逻辑优化：放弃的任务不再阻碍底线达成，连续达成天数与周达成率统计自动排除放弃的任务

## v5.0.253 (2026-08-27 11:30)

### 优化
- 全平台弹窗/浮层统一毛玻璃透明度：仿真中心（仿真详情/新增仿真记录/添加仿真/评价浮层/仿真库配置）、科研导航弹窗、顶部状态栏日期与周数弹窗、头像下拉菜单、个人信息/平台文档/关于平台弹窗、论文中心弹窗、订阅中心弹窗、计划中心日历详情弹窗、导出下拉菜单等全部从固定 0.95 改为跟随模块透明度滑杆联动
- 计划底线库操作排去除底色，直接显示在主页面模块上，仅保留微显边框避免融合

## v5.0.252 (2026-08-27 11:00)

### 修复
- 修复所有弹窗半透明后整体发灰的问题（根因：遮罩层 backdrop-filter blur 虚化背景 + 黑色遮罩叠加导致；毛玻璃模式下遮罩改为极淡 rgba(0,0,0,0.05) 且去掉虚化，弹窗透出的是真实背景图而非灰色）

## v5.0.251 (2026-08-27 10:30)

### 修复
- 修复消息中心弹窗半透明后透出黑色遮罩导致整体发灰的问题（毛玻璃模式下遮罩层从 rgba(0,0,0,0.4) 减淡为 0.15）
- 计划底线库模块透明度从固定值改为跟随模块透明度滑杆联动（daily-top-actions/daily-task-item/daily-display-box/weekly-day-cell 等全部统一）

## v5.0.250 (2026-08-27 10:00)

### 修复
- 修复消息中心弹窗整体背景仍为不透明白底的问题（弹窗 .modal-content 背景从固定 0.95 改为跟随模块透明度滑杆联动，毛玻璃模式下可透出背景图）

## v5.0.249 (2026-08-27 09:30)

### 修复
- 修复消息中心毛玻璃透明度不生效的问题（CSS 变量 --module-opacity 原先只绑定在 main-content 上，消息弹窗在 main-content 外部无法继承，现将变量提升至 app-container）

## v5.0.248 (2026-08-27 09:00)

### 优化
- 消息中心卡片透明度改用与平台设置保存栏一致的毛玻璃效果（跟随模块透明度滑杆 + blur(14px)）
- 计划底线库顶部操作排透明度降低，更透出背景图但保持可见边框不与主页面融合

## v5.0.247 (2026-08-26 22:30)

### 修复
- 修复顶部状态栏三种效果切换后实际都返回"不透明"的问题（setTopBarEffect 误用数组索引接收字符串导致值恒为 opaque）

## v5.0.246 (2026-08-26 22:00)

### 修复
- 修复平台设置中顶部状态栏效果切换后保存无响应的问题（旧代码残留导致 saveAll 运行时报错中断）

## v5.0.245 (2026-08-26 21:30)

### 新增
- 顶部状态栏效果改为三态切换：不透明（纯白底色）、毛玻璃（可调透明度+渐变融合带随透明度自动加宽）、融合态（去掉底色，logo/标题/头像等直接悬浮在背景图上）

## v5.0.244 (2026-08-26 21:00)

### 新增
- 顶部状态栏新增毛玻璃开关：平台设置新增「顶部状态栏效果」，毛玻璃模式下背景图延伸至状态栏区域，底部与内容区自然渐变融合

### 修复
- 消息中心卡片透明度降低，更透出背景图

## v5.0.243 (2026-08-26 20:00)

### 修复
- 计划库添加计划时，填写了文字的子任务自动标记"待处理"状态（黄色填充），无需手动设置
- 登录界面适配毛玻璃效果：登录卡片改为半透明毛玻璃，背景遮罩模糊增强
- 消息中心毛玻璃完善：未读+类型组合态、hover 态全部半透明化，不再有不透明穿帮

## v5.0.242 (2026-08-26 19:00)

### 修复
- 计划中心每日打卡「计划底线库」顶部操作排（今日底线任务/今日总结/本周总结添加）适配毛玻璃效果，不再纯白填充
- 每日打卡各版块（底线任务、今日总结、本周总结）边界清晰化：任务条目/展示框/周历格子/周统计块改为半透明白底+可见边框，避免毛玻璃下边界不清
- 平台设置三大模块（数据管理、easyScholar API 配置、偏好设置）适配毛玻璃效果，与全局风格统一

## v5.0.241 (2026-08-26 18:00)

### 修复
- 统一窗口设计规范：减小模块内容上方空白（page-content 上 padding 24px→10px、tab-bar 与内容区间距 14px→8px），整体内容上移，所有二级导航栏所属窗口统一
- 仿真库列表增加标题栏：新增「仿真库」标题，设置/导出/添加按钮统一在标题栏同一行，排序搜索区移至下方，与计划库/论文阅读库等布局一致
- 修复更新流程：安装过程中旧窗口不再重新打开。bat 脚本改用 start /WAIT 等待 NSIS 安装向导完成，不再无脑重启旧 exe，由安装向导 Finish 页面负责启动新版本

## v5.0.240 (2026-08-26 17:00)

### 修复
- 全面毛玻璃适配：消息中心、科研中心（论文卡片/组会纪要/节点圆点）、仿真中心（弹窗/评价浮层/详情内层卡片）、论文中心（状态弹窗/标签建议/颜色面板/符号面板/公式弹窗及内层输入区）、订阅中心添加订阅弹窗、计划中心日历详情弹窗、财务中心类型切换、导出下拉菜单等弹窗与浮层在毛玻璃模式下统一呈现半透明毛玻璃效果，不再纯白底
- 内层卡片同步半透明化，避免「卡片套卡片」视觉冲突；弹窗透明度固定较高不透明度，保持内容可读、边界清晰
- 顶部状态栏「自定义日期与周数」改名为「日期与周数」（毛玻璃与不透明模式均生效）

## v5.0.239 (2026-08-26 15:00)

### 修复
- 修复弹窗变灰问题：弹窗透明度与内容区解耦，弹窗固定较高不透明度（不再受「模块内容透明度」滑杆影响），避免透出黑色遮罩导致弹窗整体变灰/发暗
- 顶部状态栏组件适配毛玻璃：日期选择弹窗、头像下拉菜单、关于平台、平台文档、个人信息等弹窗在毛玻璃模式下同样呈现半透明毛玻璃效果，与整体风格统一

## v5.0.238 (2026-08-26 14:00)

### 修复
- 修复计划库单元格弹窗（状态/分类、层级、优先级）错位：backdrop-filter 创建新层叠上下文导致 fixed 定位偏移，改用 Teleport 到 body 修复
- 修复订阅中心添加订阅弹窗位置偏移：弹窗缺少 position:relative 且被层叠上下文影响，改用 Teleport 到 body + 补充 position:relative 修复
- 日历视图适配透明UI：PlanCalendar 组件 9 处硬编码白色背景改为透明/半透明

### 新功能
- 主页面效果切换：平台设置新增「毛玻璃/不透明」开关。不透明模式恢复纯白底色（仅导航栏透明），毛玻璃模式为半透明悬浮效果
- 弹窗适配：毛玻璃模式下弹窗也呈现半透明毛玻璃效果

## v5.0.237 (2026-08-26 11:00)

### 模块内容悬浮透明
- 全部模块内容区（计划库、科研中心、仿真中心、论文中心、计划中心、科研导航、财务中心）改为悬浮独立模块：圆角+毛玻璃+阴影，与二级导航栏风格一致
- 模块透明度可调：平台设置新增「模块内容透明度」滑杆（默认 88%，最低 30%），可透出背景图
- 内部卡片透明化：避免「卡片套卡片」，由内容区统一承载悬浮外观

## v5.0.236 (2026-08-26 10:30)

### 二级导航栏
- 压缩导航栏整体高度：上下留白由 14px 减至 9px（约减 1/3），更紧凑
- 导航栏更贴近顶部状态栏：上方留白减半，悬浮模块与状态栏间距更近

## v5.0.235 (2026-08-26 10:00)

### 二级导航栏
- 修复悬浮独立模块样式未真正生效：原因是非 scoped 全局样式被误放入 `<style scoped>` 块，选择器附加 data-v 限定无法命中子组件；已迁移到独立非 scoped 样式块，毛玻璃 + 18px 圆角 + 阴影 + 透明度在所有页面真正生效
- 二级导航栏吸顶固定：页面滚动时 Tab 栏固定在视口顶部下方，始终可见

### 窗口拖拽
- 修复窗口缩小后无法拖动顶部状态栏：Tauri 2 的 core:window:default 不含 start-dragging 权限，已新增 core:window:allow-start-dragging；前端补充 getCurrentWindow().startDragging() 兜底

## v5.0.193 (2026-08-19 16:20)

### 平台设置
- 修复「软件不存在」问题：网页端浏览器安全限制导致文件选择只能拿到文件名（拿不到完整路径），已解决
- 自定义软件新增「自动检测」：一键扫描本机已安装的 PDF 阅读器（SumatraPDF / Adobe Acrobat / Reader / Foxit / PDF-XChange / WPS / 万兴 PDFelement / Edge / Chrome / Firefox 等），点击即填入完整路径
- 新增「验证」按钮：保存前实时校验软件路径有效性，无效时给出明确指引
- 软件路径智能解析：支持软件名（自动查找安装位置/PATH）、完整路径、带引号路径、%环境变量%、目录（自动补 exe）、.lnk 快捷方式
- 桥接服务新增 `GET /find-apps`（检测已装 PDF 阅读器）与 `GET /check-app?path=`（校验路径）接口；`open-blob` 报错信息更友好（明确告知如何填写完整路径）

## v5.0.192 (2026-08-19 15:50)

### 平台设置
- 修复本地桥接服务启动器乱码报错（`'cho'/'?echo' 不是内部或外部命令`）：启动脚本改为 GBK 编码，兼容 Windows cmd 默认代码页
- 桥接服务脚本改名 `server.cjs`，避免项目 `type: module` 环境下 Node 按 ES Module 解析导致启动失败
- PDF 保存目录支持自定义：设置页可直接填写绝对路径保存，服务在线时自动写入 `bridge-config.json`；也可手动编辑该文件
- 保存目录优先级：网页调用参数 > `bridge-config.json` > 默认 `文档/WorkbenchPDF`
- 桥接服务新增 `GET/POST /config` 接口，用于读写保存目录配置

## v5.0.191 (2026-08-19 15:30)

### 论文中心
- 标签选择面板改为输入框下方的下拉形式（贴近输入框，聚焦即弹出，支持筛选/创建），不再悬浮在页面底部
- 论文库配置新增「删除标签」：一键清除文献与阅读记录中的该标签及颜色配置
- 论文阅读模块新增排序选项（按添加时间/标题/年份 + 升降序）与标签筛选；默认规则保持「后添加的在前」
- 添加阅读记录支持选择已有标签（与文献一致：面板选择 + 回车创建），阅读设置新增「阅读标签管理」：调整颜色、上移/下移排序、删除标签
- 标签排序全局生效（文献与阅读记录的标签面板/筛选按自定义顺序展示）

### 每日打卡
- 今日底线任务增加「未完成」按钮（位于「标记完成」之后），可将已完成任务一键改回未完成

### 平台设置
- PDF 打开方式新增「本地桥接服务」方案：浏览器安全限制下也可一键调用本地软件打开 PDF（下载 server.js + 启动器，本地运行后自动检测在线状态）

## v5.0.190 (2026-08-19 11:30)

### 论文中心
- 添加/编辑文献的「标签」支持选择已有标签：聚焦或输入时弹出已有标签面板，点击即可快速关联，无需重复手打
- 输入未存在的标签并回车仍按原逻辑创建新标签（自动分配默认颜色），面板底部同时给出「创建标签」入口
- 已选标签在面板中自动隐藏，输入可实时筛选已有标签

## v5.0.189 (2026-08-19 11:00)

### 论文中心
- 论文库新增「PDF 本地关联」能力：添加/编辑文献时可选择 PDF 文件，自动保存到「平台设置 → 论文 PDF 存放位置」配置的目录，未配置时自动存入浏览器本地数据库（IndexedDB，即 Web 端 data 文件夹），同样可一键打开
- 论文列表行与详情弹窗均提供「打开 PDF」按钮；未关联 PDF 的文献不显示，关联后自动可用
- 「打开方式」支持跳转到自定义阅读软件：桌面版（Tauri）可直接启动本地 exe；网页版受浏览器安全限制只能下载并由系统关联程序打开，已明确提示
- 修复此前手动添加文献无法打开 PDF 的问题：根因为表单缺少独立 PDF 关联字段，现已补齐并支持关联/移除/重新选择

## v5.0.188 (2026-08-19 10:30)

### 论文中心
- PDF 自动解析大幅增强：识别到 DOI 后自动请求 Crossref 学术数据库反查权威元数据，题目、作者、期刊、年份、摘要按出版商注册信息精准补全，不再依赖正则「猜」
- Crossref 命中时提示语标注「Crossref 已补全」，补全的期刊名会继续触发影响因子/分区自动查询
- 网络异常、跨域或限流时自动回退到原有本地正则解析，导入流程不会因此中断

## v5.0.187 (2026-08-18 17:25)

### 消息中心
- 修复推送版本号错误问题：更新推送强制只显示最新版本，历史残留的旧版本推送（如 3.0 等）在启动时自动清理，且渲染层双重过滤，任何情况下都不会再看到非最新版本的更新消息
- 推送前校验 + 消息去重：重复/低版本推送在源头丢弃，杜绝消息中心出现多条版本号不一致的更新推送

## v5.0.186 (2026-08-18 17:15)

### 消息中心
- 修复更新推送问题：只推送最新版本的更新内容，不再一次性推送大量历史更新
- 已积压的历史版本更新消息会在启动时自动清理，仅保留最新一条

## v5.0.185 (2026-08-18 16:40)

### 论文中心
- 新增「论文阅读」模块（位于总览与论文库之间）：记录阅读论文的研究对象、研究目的、研究方法、创新点、可借鉴思路等细节
- 阅读记录可链接论文库中的论文，单击记录弹窗展示关联论文详细信息（作者、期刊、状态、影响/分区、DOI、PDF 打开与批注）
- 支持按论文名、时间、标签搜索；默认按添加顺序降序（后添加在上），可切换升序
- 阅读设置：表格列显示勾选、列顺序调整、自定义字段（文本/数字/多行）增删
- 导出支持 Markdown 与 PDF
- 研究对象、研究目的、研究方法支持多行输入；阅读记录详情长文本自动换行，修复标题遮挡

## v5.0.184 (2026-08-18 14:15)
- 欢迎页面逻辑优化：平台未登录时不显示，当天首次登录成功后才弹出欢迎页面
- 每次版本更新后消息中心自动推送更新提醒卡片，附带「查看更新日志」跳转按钮

## v5.0.183 (2026-08-18 10:56)
- 全平台装饰性 Emoji 统一替换为 Lucide SVG 图标库，统一图标尺寸（18px）、线宽（2px）和颜色（currentColor）；功能性符号（✓✕等）与数学符号保留
- 新增 AppIcon 组件 + icons.js 集中映射，全局注册；修复 AppIcon 未注册导致图标不渲染的阻断性 bug
- 导航栏、登出菜单、消息中心、日期选择器、网站导航、计划配置组、积分商店等全部渲染器统一改用 AppIcon
- 全平台响应式布局改造：桌面多列 → 平板减列 → 手机单列，确保文字、图片和操作区完整不溢出（覆盖 Plan/PaperCenter/Simulation/PointsCenter/CalendarView/PlanCalendar/App.vue 等）

## v5.0.182 (2026-08-18 09:10)
- 🗓 每日打卡三个添加弹窗（底线任务/今日总结/本周总结）右上角保存左侧新增日期/周选择器，可补录往日或提前安排；底线任务添加仅新增模式可选日期
- 🔗 今日总结「明日计划」保存后，自动按行生成次日「今日底线任务」，任务行标注「来自昨日明日计划」；重复保存不累积

## v5.0.181 (2026-08-17 11:21)
- 🧪 组会纪要头部重排：日期与时间一行靠左、编辑/删除两按钮同行靠右；参与人员另起一行，采用与下方汇报内容一致的字段格式；卡片统一拉伸为等高，整行记录模块长度保持一致

## v5.0.180 (2026-08-17 11:09)

### 科研中心
- 组会纪要布局修正：每条纪要作为整体一个底色卡片，页面按现有宽度左右分布，一行展示五条纪要（超出自动换行），窄屏逐级降为 4/3/2/1 列

## v5.0.179 (2026-08-17 10:54)

### 科研中心
- 组会纪要单条展示改为竖向卡片一行五列，字段标题置顶、内容区独立
- 培养节点顶部删除「+ 添加节点」按钮，节点统一通过「阶段配置」管理

## v5.0.178 (2026-08-17 11:30)

### 新增 / 优化
- 🧪 科研中心：论文管理新增「论文状态」自定义（增删改状态与颜色、排序）；论文管理 / 组会纪要 / 培养节点均新增导出（Markdown / PDF）
- 🧪 组会纪要列表由两行一列改为一行五列展示
- 🧪 培养节点改为弹窗编辑，并支持自定义阶段（名称、颜色、排序、恢复默认）
- 🧭 网站导航顶部重绘为统一头部（标题 + 手动排序 / 新建分类 / 添加网站按钮），统计数字下移为独立卡片
- 🗑 移除「灵感捕捉」功能及其页面、积分项与平台文档相关描述

## v5.0.177 (2026-08-17 09:34)

### 优化
- **日历月视图本月以外日期淡化**：计划中心日历月视图中，本月以外的日期数字与星期副标签改用三级灰文字并降低透明度，不再与本月日期抢视觉；计划条目保持原色仍可清晰辨识。

## v5.0.176 (2026-08-17 09:30)

### 新增
- **底线任务编辑入口**：已添加的底线任务支持修改部分信息（任务主题、任务描述、关联计划）。任务行新增编辑按钮，详情弹窗中也增加「编辑」按钮，二者均复用添加弹窗并预填当前任务内容；保存时调用 updateCheckinTask 更新对应日期任务。

## v5.0.175 (2026-08-16 22:00)

### 优化
- **每日打卡顶部独立模块**：顶部「计划底线库」标题 + 三个快捷按钮区域增加白色卡片底层（白底 + 圆角 + 边框 + 阴影），作为独立模块与下方内容分隔。

## v5.0.174 (2026-08-16 21:10)

### 修复
- **总结弹窗残留旧内容**：今日总结 / 本周总结弹窗每次打开时重置为空白表单，不再残留上次填写的内容。

### 优化
- **顶部布局调整**：三个快捷按钮（今日底线任务添加 / 今日总结添加 / 本周总结添加）移至同一行右侧，左侧新增「计划底线库」标题。
- **历史日期切换**：今日底线任务、今日总结支持前后天切换查看历史数据（非当天显示「回到今天」按钮）；本周总结支持前后周切换查看历史周数据（非本周显示「回到本周」按钮），周达成率等统计随查看周联动。

## v5.0.173 (2026-08-16 20:40)

### 优化
- **每日打卡顶部快捷按钮**：计划中心「每日打卡」最上方新增一行快捷操作按钮（今日底线任务添加 / 今日总结添加 / 本周总结添加），采用简笔画图标 + 文字风格，点击后分别打开对应弹窗填写内容。
- **今日总结改为信息展示**：今日总结区域移除右上角「保存今日总结」按钮，原输入框变为展示框，展示已保存的今日工作进展、遇到的问题、明日计划。
- **本周总结改为信息展示**：本周总结区域移除「本周工作进展」和「下周计划」输入框，改为展示已保存内容；保留本周七天达成率、周统计卡片。
- **关闭按钮样式统一**：弹窗关闭按钮改为圆角矩形内「×」文字图标，参考示例图样式。

### 删除
- **每日打卡历史记录**：移除每日打卡底部「历史记录」区域（日总结 / 周总结切换列表）。

## v5.0.172 (2026-08-16 20:20)

### 优化
- **计划/任务配置图标重绘**：计划中心「计划/任务配置」弹窗中「计划配置」与「任务配置」模块标题图标由 emoji 改为简笔画线条风格（日历页 / 待办清单），并移除重复的配置提示文字。
- **日历月视图重新设计**：计划中心日历月视图参考新版日历布局重新设计，日期数字加大加粗，下方增加星期简写副标签（今日显示「今日」），计划条改为通栏圆角色块，整体采用浅色主题配色。

### 新增
- **GitHub Release 自动发布**：新增 `npm run release` 脚本，部署推送后自动从 CHANGELOG.md 提取当前版本日志创建 GitHub Release（标题为 Workbench + 版本号）。

## v5.0.171 (2026-08-16 20:00)

### 优化
- **仿真库头部 UI 重绘**：仿真中心「仿真库」表格头部由「列设置 / 状态设置 / 添加仿真」改为「仿真库配置 / 导出 / 添加仿真」。列设置与状态管理合并进「仿真库配置」弹窗（2×2 网格），三个头部按钮均为简笔画图标 + 文字风格。
- **仿真库新增导出功能**：支持导出 Markdown 与 PDF（导出当前筛选后的仿真记录）。
- **计划库配置入口外移**：计划中心「计划库」将「添加计划」弹窗内的「计划/任务配置」按钮移出到表格 header 左侧，新增「导出」按钮，header 顺序统一为「计划/任务配置 / 导出 / 添加计划」。
- **计划库新增导出功能**：支持导出 Markdown 与 PDF（导出当前筛选后的计划）。

## v5.0.170 (2026-08-16 18:00)

### 优化
- **论文库配置弹窗布局优化**：四个模块（状态管理、表单字段、表格列显示、标签颜色）由单列改为 2×2 网格布局，配置弹窗最大宽度加宽至 920px。
- **论文库配置图标重绘**：四个模块标题图标统一改为简笔画线条风格（状态/表单/表格/标签）。

## v5.0.169 (2026-08-16 17:00)

### 优化
- **论文库影响列支持中文期刊级别**：新增 CSSCI、北大核心、CSCD 级别徽章显示，easyScholar 查询结果中的 CSCD 字段也会拼入 impact 字符串。

## v5.0.168 (2026-08-16 20:00)

### 优化
- **论文库题目列宽度精确化**：题目列最大宽度改为与「基于制动特性的路面附着状态识别研究」等宽，超出部分隐藏并显示省略号。
- **修复表格行点击无详情弹窗**：修复论文库表格行单击无法打开文献详情弹窗的问题。
- **论文库头部按钮重绘**：论文库配置、导出、添加文献三个右上角按钮改为「简笔画图标 + 文字」风格；论文库配置与导出底层为白色透明圆角矩形，添加文献保留紫色主按钮。
- **文献详情弹窗按钮风格统一**：右上角编辑按钮改为紫色描边 + 图标 + 文字的轻盈风格，关闭按钮改为灰色圆角 X。

## v5.0.167 (2026-08-16 19:30)

### 优化
- **论文库配置入口调整**：将「论文库配置」从添加/编辑文献弹窗中移出，放置在论文库表格右上角「添加文献」按钮左侧，入口更直观。
- **标签颜色改为选择设置**：新增「论文库配置 → 标签颜色管理」区块，所有标签支持通过颜色选择器自定义颜色；移除添加/编辑文献弹窗中「点击标签圆点循环切换颜色」的行为，颜色统一在配置中管理。
- **论文题目列宽度调整**：论文库表格题目列最大宽度改为 16 个字符，超出部分自动隐藏并显示省略号。
- **文献详情弹窗优化**：点击论文库表格行可打开文献详情弹窗，弹窗右上角保留编辑按钮，采用紫色主按钮 + 灰色关闭按钮风格。

## v5.0.166 (2026-08-16 15:30)

### 优化
- **论文库表格优化**：
  - 论文题目列宽度调整为 310px~420px（基于 16 字标题 + 左右各 3 字余量）
  - 影响列改为徽章显示，仅展示 IF、JCR 分区、中科院分区（中科院大类名称如"工程技术"自动缩写为"中科院"）
  - 操作列仅保留编辑和删除，移除 PDF 按钮以简化界面
  - 标签支持自定义颜色：添加标签时自动分配颜色，点击标签前圆点可循环切换 10 种预设颜色
  - 筛选标签条和详情弹窗同步显示标签颜色

## v5.0.165 (2026-08-16 14:00)

### 优化
- **移除「升级/导入快照（浏览器缓存）」功能**：localStorage 快照受 5-10MB 限制易占满存储导致保存失败，现已移除 UI 和写入逻辑。
  - 升级/导入前仍自动写快照到本地文件夹（`snapshot_*.json`，保留 10 份），数据保护不受影响。
  - 代码版本回退由 GitHub tag 管理（`git checkout v5.0.16X` → 重新构建部署）。
  - 清除 localStorage 中残留的旧快照数据，释放浏览器存储空间。
- **每日打卡底线任务优化**：关联计划时任务主题可自由输入，列表行以紫色底色标签显示关联计划名称；计划改名自动同步，计划删除显示灰色提示。
- **底线任务详情弹窗**：点击任务行打开，可查看任务描述、关联计划信息（层级/状态/进度/截止日期），可直接切换完成状态。

## v5.0.164 (2026-08-16 10:00)

### 新增
- **计划中心「每日打卡」模块**：插在「总览」与「计划库」之间，帮助研究生守住每日任务底线。
  - 每日底线任务：支持从计划库关联计划，也可手动添加临时任务；勾选完成，全部完成即「底线达成」，显示连续达成天数。
  - 每日总结：今日工作进展、遇到的问题、明日计划三个输入项。
  - 每周总结：展示本周七天达成情况、本周达成率、最长连续天数、完成底线任务数；填写本周进展与下周计划。
  - 历史记录：底部浮起卡片，可切换查看日总结/周总结。
  - 顶部 Tab 切换样式统一为胶囊型紫色高亮，与截图风格一致；底线任务列表行样式与近期任务列表保持一致。

### 联动
- **消息中心**：每日 22:00 后如有未完成的底线任务推送提醒；每周日 20:00 后如未填写本周总结推送提醒。
- **总览统计**：新增「连续打卡」和「本周达成率」两个统计卡片，点击跳转每日打卡页。

## v5.0.163 (2026-08-15 20:00)

### 优化
- 仿真中心总览数字字体由等宽字体改为 Arial

## v5.0.162 (2026-08-15 18:30)

### 修复
- **PWA 登录界面无限刷新**：
  - 根因：`reconcileWithFileStorage` 在启动时将文件中旧的 `authLoginAt`（时间戳）和 `authUsername` 拉回 localStorage（dataScore 比较"更全"），触发 `window.location.reload()`；刷新后 `checkSession()` 因 `sessionStorage` 为空再次 `logout()`，循环往复。
  - 修复一：`authLoggedIn` / `authUsername` / `authLoginAt` 三个会话级字段不参与文件合并。
  - 修复二：PWA standalone 模式下跳过 `sessionStorage` 检查（PWA 每次启动 sessionStorage 都是空的，靠 24h 超时兜底安全）。
  - 修复三：新增 `mw_reloadGuard` 计数器，同一会话内最多允许 2 次自动 reload，杜绝任何场景下的刷新死循环。

## v5.0.161 (2026-08-14 21:30)

### 新增
- **主页全新改版**：
  - 顶部「学业进度总览」宽度自适应，保持原有格式。
  - 新增四列概览卡片：论文概览、最近仿真、最近论文、最近组会，分别展示科研论文进度、近期仿真记录、最近阅读的文献、最近组会纪要。
  - 底部左侧为「最近计划」周日历网格，右侧为「计划完成情况」环形占比图。
  - 各卡片均支持点击进入对应模块。

## v5.0.156 (2026-08-14 15:35)

### 新增
- **仿真中心仿真库状态自定义**：状态名称与颜色均可自定义。
  - 工具栏「+ 添加仿真」旁新增「状态设置」入口：可修改内置状态（已完成 / 仿真中 / 待开始 / 取消）的名称与颜色，也可新增自定义状态、删除自定义状态、一键恢复默认。
  - 修改立即生效：总览统计卡片、状态分布图、仿真库表格、详情页与添加/编辑弹窗同步更新，不影响已有仿真的状态数据。

## v5.0.155 (2026-08-13 16:35)

### 修复
- **PWA 安装版后台恢复后导航卡顿**：窗口放置后台一段时间后切回前台，点击左侧导航没反应、约 1 分钟才跳转的问题。根因是浏览器冻结后台页面后，恢复前台瞬间一次性补执行积压的定时器回调（专注/倒计时每秒 tick + 写盘），主线程被占满导致点击事件排队。
  - **计时器时间补偿**：专注/倒计时计时器改为按 `Date.now()` 实际流逝时间一次性补计，后台冻结期间不再堆积 tick 与写盘任务。
  - **文件写入合并队列**：同一数据键的高频写入合并为最后一次并串行落盘，避免 `createWritable` 并发排队卡住界面。
  - **自动备份空闲执行**：自动备份改在 `requestIdleCallback` 空闲时段执行，不再抢占恢复瞬间的主线程。
  - **主页面预加载**：Dashboard 改为静态引入，恢复前台无需等待懒加载网络请求。

## v5.0.154 (2026-08-13 15:45)

### 新增
- **自动备份可开关 + 内容改动自动备份**：平台设置「自动备份」新增开关，默认开启。开启状态下，任何数据内容修改后 3 秒防抖自动写入 `backups/` 目录，无需手动点击「立即备份」；状态文案实时显示最近自动备份时间，备份结果一目了然。

### 变更
- **PWA 安装版顶部标题栏颜色**：由深蓝色改为 `#F0F8FF`（AliceBlue 浅蓝），与浅色界面更协调。云端与已安装的 PWA 同步更新，无需重新安装。

## v5.0.153 (2026-08-12 21:16)

### 修复
- **计划库子任务弹窗置顶**：修复计划中心计划库子任务弹窗保持窗口在最上面，现在会出现被遮挡现象。弹窗通过 `<Teleport>` 挂载到 `body` 根节点，层级（z-index）提升至 99999；当子任务单元格靠近视口底部、下方空间不足时，弹窗自动向上展开，避免被截断。

## v5.0.152 (2026-08-12 21:08)

### 改进
- **子任务新增截止时间**：点击计划库子任务单元格，在颜色选择器的「自定义」下方新增「截止时间」选择，支持年月日时分秒，用横线与下方「清除填充」隔开，结构清晰；选择后时间以淡色小字显示在单元格底部，格式 MM-DD HH:mm
- **单元格适配填充色**：子任务单元格高度从 28px 增加到 44px，容纳任务名 + 时间；已填充背景色时，文字和时间自动根据背景亮度切换深/浅色，确保任意填充色下都能看清。

## v5.0.151 (2026-08-12)

### 修复
- **编辑仿真记录时拖选文本误关弹窗**：修复了在输入框（如「仿真文件存放位置」）内按住鼠标从右向左拖动选择已有内容、鼠标移到左侧空白处松开时弹窗被意外关闭的问题。现在只有「按下和松开都在弹窗外空白处」才关闭窗口；从输入框内拖选文本松手到空白处不会关闭，可放心选中删除后重新输入。新增 / 编辑仿真弹窗同步修复。

## v5.0.150 (2026-08-12)

### 改进
- **仿真详情输入更安全**：在「仿真详情」文本框中填写内容时，单击弹窗外的空白处不再关闭窗口，必须点击右上角「×」才能关闭，避免误触丢输入。表单无修改时空白处点击照常关闭。
- **仿真记录新增「标注理由」字段**：在每条仿真记录卡片的「标注」与「编辑」之间增加一个中间区域显示标记理由（例如「收敛曲线与参考论文一致 → 保留」「网格划分错误导致能量不守恒 → 舍弃」），点击右侧「编辑」可填写。

## v5.0.149 (2026-08-12)

### 新增
- **数据管理全面重构（单文件夹方案）**：主存储与备份从「两个分离配置」合并为「一个文件夹」，彻底消除漏配备份导致数据裸奔的结构性风险。文件夹内含 `data/`（当前数据）、`backups/`（历史备份+快照+手动备份）、`attachments/`（PDF/头像）、`exports/`（导出留档）、`meta.json`。
- **自动备份内置**：每次修改数据自动备份到 `backups/` 目录（自动备份保留 20 份、升级/导入前快照保留 10 份、手动备份保留 20 份），无需单独配置备份文件夹。
- **启动智能合并**：本地文件夹（主存储）与浏览器缓存（渲染镜像）双向取更全的一侧——缓存缺失/损坏以文件为准，文件更全回灌缓存，缓存更全写回文件；任何一侧清空/损坏都不会拖累另一侧，从机制上杜绝数据丢失。
- **文件版回退**：数据管理页的历史备份列表支持一键恢复，恢复前自动创建「回退备份前（自动快照）」防误操作；恢复采用合并式写入，备份中不存在的模块不会删除。
- **导出详情**：导出后弹窗明确展示每个模块的名称、条数（数组长度/对象键数）与体积（按体积降序），清楚知道导出了什么。
- **未绑定文件夹提示**：数据仅存浏览器缓存时，顶部显示蓝色横幅「建议绑定本地文件夹并启用自动备份」，点击直达平台设置，防止电脑关闭/清缓存导致数据丢失。
- **兼容旧方案**：旧的独立自动备份文件夹（`auto_backup_*.json`）仍可读取恢复。

### 技术说明
- 涉及文件：`src/utils/fileStorage.js`（单文件夹目录结构 + 备份 API：`writeMainBackup`/`listMainBackups`/`readMainBackup`/`deleteMainBackup`/`restoreMainBackup` + `fileWriteRaw`）、`src/utils/storage.js`（`save()` 三路写入 + 统一备份封装 + `reconcileWithFileStorage` 智能合并 + `formatBytes`）、`src/stores/index.js`（导出详情 `detail` + 文件版快照 + `restoreFromFileBackup`）、`src/views/Settings.vue`（自动备份内置卡片 + 历史备份列表 + 导出结果弹窗 + 恢复确认弹窗）、`src/App.vue`（未绑定提示条 + 启动智能合并 + 5.0.149 播种）、`package.json`、`CHANGELOG.md`

## v5.0.148 (2026-08-12)

### 修复
- **计划数据丢失的真正根因（升级后丢失）**：v5.0.147 的版本回滚快照会把**全部** `mw_*` 数据（含头像 base64、PDF 存储、操作日志、甚至上一份快照自身）复制一份，最多 3 份全量快照 + 数据本体，极易撑爆浏览器 5MB 存储上限。存储写满后所有保存静默失败（新增/编辑计划刷新即丢）。已修复：
  - 快照**瘦身**：排除头像、背景图、PDF 存储、操作日志、消息中心、提醒标记、自动备份临时键等大体积/低价值字段，并**禁止快照嵌套**（不再包含上一份快照），单份快照体积大幅下降
  - **存储预警**：写入仍失败时明确提示「进入平台设置删除多余的版本回滚快照」
- **数据自愈**：启动时自动检测损坏的数据键（值为 `'null'`/`'undefined'`/空串/损坏 JSON，此前会导致计划中心空白），优先从本地文件夹恢复，恢复不了则清除坏数据并回退默认值，页面不再空白，且不再把坏数据继续写回
- **导入空值不再覆盖**：导入 JSON 时值为 `null`/`undefined`/空串的模块自动跳过（弹窗中标记「⏭ 跳过」），不再用空值覆盖现有数据
- **加载防御**：`load()` 遇到 `'null'`/`'undefined'` 字符串返回默认值；`saveSync()` 拒绝写入 `null`/`undefined`，从源头阻止数据被污染成 `'null'` 后读不出来

### 变更
- **快照可单独删除**：版本回滚列表中每条快照新增「🗑 删除」按钮，可手动释放存储空间（删除后不可回退到该时间点）

### 技术说明
- 涉及文件：`src/stores/index.js`（快照瘦身 + 删除快照 + 导入跳过空值 + 回滚防御）、`src/utils/storage.js`（`load`/`saveSync` 空值防御）、`src/App.vue`（数据自愈 + 5.0.148 播种）、`src/views/Settings.vue`（快照删除按钮 + 导入弹窗 skipped 状态）、`package.json`、`CHANGELOG.md`

## v5.0.147 (2026-08-11)

### 新增
- **版本回滚**：每次升级版本与导入 JSON 前，平台自动将当前完整数据保存为快照（最多保留 3 个）。对某次更新不满意时，可在「平台设置 → 数据管理 → 版本回滚」一键回退，快照未包含的模块不会删除，**数据不丢失**。
- **导入 JSON 结果确认弹窗**：导入后弹窗明确显示导入的模块列表、成功/失败数量；导入文件中没有的模块原样保留（不再清空删除）。

### 修复
- **计划中心数据丢失（100% 复现）**：此前导入会先清空全部 `mw_*` 数据再写入备份内容，若备份不完整或为旧格式，计划等数据会被清掉；且本地文件夹同步会用旧文件覆盖新数据。现已改为：
  - 导入采用**合并模式**——只覆盖备份文件中存在的模块，其余数据保留
  - 本地文件夹同步改为**只补缺、不覆盖**——仅当 localStorage 缺失（或为空且文件有数据）时才从文件恢复，杜绝旧文件覆盖新数据
- **论文中心数据刷新不出来**：与计划中心同根因（数据被导入/文件夹同步清空），共用上述安全修复，文献数据不再被意外清空。

### 变更
- **仿真记录评价标签**：仿真详情中每条记录标题处新增「标注」按钮（与编辑/删除按钮同风格），单击弹出小窗口选择「保留（绿底）/ 舍弃（黑底）/ 暂定（黄底）」，选择后标题位置显示对应彩色评价标签，可随时单击修改或取消标注。评价随记录保存至 `record.evaluation` 字段。

### 技术说明
- 涉及文件：`src/stores/index.js`（合并式导入 + 回滚快照 + 仿真记录评价字段）、`src/utils/storage.js`（`syncFromFileStorage` 合并同步）、`src/views/Settings.vue`（导入结果弹窗 + 版本回滚入口）、`src/views/Simulation.vue`（评价标签 UI）、`src/App.vue`（升级前自动快照 + 版本播种）、`package.json`、`CHANGELOG.md`

## v5.0.146 (2026-08-11)

### 修复
- **头像上传提示存储空间不足**：头像原图直接转 base64 存入 localStorage，单张图片可达数 MB，极易触顶。上传后先通过 canvas 压缩为 256×256、JPEG 0.8，通常可降至几十到几百 KB，避免占满浏览器存储。

### 变更
- 顶部头像弹窗（App.vue）与「个人信息」页面（Profile.vue）两处上传均接入统一压缩逻辑。

### 技术说明
- 涉及文件：`src/utils/storage.js`（新增 `compressImage()`）、`src/App.vue`、`src/views/Profile.vue`

## v5.0.145 (2026-08-11)

### 修复
- **自动备份文件导入无效**：此前「JSON 自动备份」导出的 `auto_backup_*.json` 与「导出 JSON」格式不一致——备份文件的数据键缺少 `mw_` 前缀、值已是解析后的对象，导致导入时写入 localStorage 的键名错误，应用读取不到，数据"没有回来"。现已统一两种格式，并强化导入逻辑：
  - 自动备份写入格式与「导出 JSON」完全一致（键带 `mw_` 前缀 + 序列化字符串值），今后生成的备份文件可直接导入恢复
  - 导入逻辑兼容三种格式：带前缀备份 / 无前缀备份（含历史 `auto_backup_*.json`，键自动补 `mw_` 前缀，值自动序列化）/ 扁平格式
  - 已绑定本地存储文件夹时，导入后自动同步数据到硬盘文件夹

### 变更
- **停止维护 EXE 桌面版**：从本版本起仅更新云端平台（`https://c99f925959c74ddaa1594985e09b3d86.app.codebuddy.work`），EXE 不再随版本更新

### 技术说明
- 涉及文件：`src/stores/index.js`（导入兼容 + 文件存储同步）、`src/utils/storage.js`（新增 `exportAllRaw()`，自动备份改用统一格式）
- 云端地址：`https://c99f925959c74ddaa1594985e09b3d86.app.codebuddy.work`

## v5.0.144 (2026-08-11)

### 修复
- **桌面版导出 JSON 不可用**：EXE 内 WebView2 静默拦截浏览器下载，导出备份文件无响应。新增双环境保存通道——桌面端弹 Windows 原生「另存为」对话框保存到任意位置，浏览器端保持原有下载行为
- **操作日志 CSV、平台文档 HTML 导出**：同步支持桌面端原生保存

### 技术说明
- 前端：`src/utils/storage.js` 新增 `saveTextToFile()`（检测 `window.pywebview.api`），改造 `Settings.vue` 导出 JSON、`stores/index.js` 操作日志 CSV、`App.vue` 平台文档 HTML
- 桌面壳：`app.py` 新增 `JsApi.saveTextFile()`（ctypes 调 `GetSaveFileNameW` 原生对话框），通过 `js_api` 注入页面
- 云端地址：`https://c99f925959c74ddaa1594985e09b3d86.app.codebuddy.work`（EXE 自动同步）

## v5.0.143 (2026-08-11)

### 新增
- **仿真结果多行输入**：新增仿真记录中"仿真结果"改为多行文本输入（与"仿真细节"一致），详情展示自动保留换行与多行格式

### 修复
- **弹窗误关丢数据**：新增/编辑仿真记录时，若点击弹窗空白处或关闭按钮，会先弹出「当前输入尚未保存」确认框，确认后才关闭，避免未保存内容丢失

### 技术说明
- 涉及文件：`src/views/Simulation.vue`
- 云端地址：`https://c99f925959c74ddaa1594985e09b3d86.app.codebuddy.work`（EXE 自动同步，无需重新打包）

## v5.0.142 (2026-08-11)

### 新增
- **Electron 桌面版支持**：打包为 Windows 便携 exe，支持离线运行
- **exe 壳 + 云端页面混合模式**：exe 加载 CloudStudio 云端 URL，代码更新只需重新部署云端，无需重新打包 exe
- **Electron 原生文件系统**：使用 `dialog.showOpenDialog` 做文件夹选择，通过 IPC 实现本地文件读写，权限更稳定

### 变更
- 移除头像下拉菜单中的"平台设置"入口（已移至侧边栏导航）

### 技术说明
- Electron 主进程文件：`electron/main.js` + `electron/preload.js`
- 双模式存储后端：浏览器 File System Access API / Electron Node.js fs
- 包输出：`release/硕士工作台-5.0.142-portable.exe`（约 68MB）
- 云端地址：`https://c99f925959c74ddaa1594985e09b3d86.app.codebuddy.work`
